{
  "name": "omnichanel",
  "version": "1.0.0",
  "description": "OTA Product Inventory Management System",
  "main": "server/index.js",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "start": "node server/index.js",
    "dev": "nodemon server/index.js",
    "build:dashboard": "cd dashboard && npm install && npm run build",
    "build": "npm run build:dashboard"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "pg": "^8.11.3"
  },
  "devDependencies": {
    "nodemon": "^3.0.2"
  }
}
